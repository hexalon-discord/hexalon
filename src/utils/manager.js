const { EmbedBuilder, TimestampStyles, MessageComponentInteraction, InteractionType, Embed, Events, ButtonBuilder, ButtonStyle, ActionRowBuilder, UserContextMenuCommandInteraction, ModalBuilder, TextInputBuilder, TextInputStyle } = require("discord.js");
const config = require("../config/config");
const { ButtonInteraction } = require("discord.js");

module.exports = class Manager {

    static createErrLog(guildId, userId, commandName, prefix, time, error) {
    }

}